network_path:
  collector:
    workers: 0
  connections_monitoring:
    enabled: false

traceroute:
  enabled: false

system_probe_config:
  enabled: false

service_monitoring_config:
  enabled: false

runtime_security_config:
  enabled: false

compliance_config:
  enabled: false

process_config:
  enabled: false
  process_discovery:
    enabled: false

cloud_provider_metadata: []

workloadmeta:
  enabled: false